import { defineComponent } from 'vue'
import t from './index.module.scss'
export const IconDemo1 = defineComponent({
    props: {},
    setup(props, context) {
        return ()=>(
            <div class={'demo-item'}>
                
            </div>
        )
    }
})